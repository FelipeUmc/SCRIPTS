-- Create GUI
local gui = Instance.new("ScreenGui")
gui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Main Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 200, 0, 260)
frame.Position = UDim2.new(0.5, -100, 0.5, -130)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
frame.BackgroundTransparency = 0.1
frame.BorderSizePixel = 0
frame.Parent = gui
frame.Active = true
frame.Draggable = true

-- Corner rounding
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = frame

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 40)
title.Position = UDim2.new(0, 0, 0, 0)
title.BackgroundTransparency = 1
title.Text = "TELEPORT MENU"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.Parent = frame

-- Best Luck Button
local luckButton = Instance.new("TextButton")
luckButton.Size = UDim2.new(0, 160, 0, 50)
luckButton.Position = UDim2.new(0.5, -80, 0, 50)
luckButton.Text = "TP TO BEST LUCK"
luckButton.BackgroundColor3 = Color3.fromRGB(100, 150, 255)
luckButton.TextColor3 = Color3.fromRGB(255, 255, 255)
luckButton.TextScaled = true
luckButton.Parent = frame

local luckCorner = Instance.new("UICorner")
luckCorner.CornerRadius = UDim.new(0, 8)
luckCorner.Parent = luckButton

-- Base Button
local baseButton = Instance.new("TextButton")
baseButton.Size = UDim2.new(0, 160, 0, 50)
baseButton.Position = UDim2.new(0.5, -80, 0, 110)
baseButton.Text = "TP TO BASE"
baseButton.BackgroundColor3 = Color3.fromRGB(150, 100, 255)
baseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
baseButton.TextScaled = true
baseButton.Parent = frame

local baseCorner = Instance.new("UICorner")
baseCorner.CornerRadius = UDim.new(0, 8)
baseCorner.Parent = baseButton

-- Instant Grab Button
local grabButton = Instance.new("TextButton")
grabButton.Size = UDim2.new(0, 160, 0, 50)
grabButton.Position = UDim2.new(0.5, -80, 0, 170)
grabButton.Text = "INSTANT GRAB"
grabButton.BackgroundColor3 = Color3.fromRGB(255, 165, 0)
grabButton.TextColor3 = Color3.fromRGB(255, 255, 255)
grabButton.TextScaled = true
grabButton.Parent = frame

local grabCorner = Instance.new("UICorner")
grabCorner.CornerRadius = UDim.new(0, 8)
grabCorner.Parent = grabButton

-- Positions
local bestLuckPos = Vector3.new(-5, -3, -34084)
local basePos = Vector3.new(28, 44, -46)

-- Teleport function
local function teleport(position)
    local character = game.Players.LocalPlayer.Character
    if character and character:FindFirstChild("HumanoidRootPart") then
        character.HumanoidRootPart.CFrame = CFrame.new(position)
    end
end

-- Instant Grab function
local function instantGrab()
    for i, v in ipairs(game:GetService("Workspace"):GetDescendants()) do
        if v.ClassName == "ProximityPrompt" then
            v.HoldDuration = 0
        end
    end
end

-- Button clicks
luckButton.MouseButton1Click:Connect(function()
    teleport(bestLuckPos)
end)

baseButton.MouseButton1Click:Connect(function()
    teleport(basePos)
end)

grabButton.MouseButton1Click:Connect(function()
    instantGrab()
end)

-- Optional: Make it always active (uncomment if you want it always on)
-- instantGrab()